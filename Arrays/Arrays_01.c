#include<stdio.h>

 int main(){
 	const float pi = 3.14;
 	float a=2.4e7;// Where a= 2.4*10^7 
 	printf("THe Float is %f\n",a);
 	printf("The const is %f\n",pi);
 	
 	char a = 1;
 	printf("the size %c\n",sizeof(a));
 	printf("The value of char is %c\n",a);
 	
 	return 0;
 }
