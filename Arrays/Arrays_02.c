//Pointer Arithmetic - 01
#include<stdio.h>
 
 int main(){
 	// int is of four bytes
 	// int = 4 bytes
 	/*
 	int i = 34;
 	int *ptr = &i;
 	printf("The value of ptr = %u\n",ptr);
 	ptr++; // (ptr = ptr + 1)
 	printf("The value of ptr = %u\n",ptr);
 	ptr = ptr + 2;
 	printf("The value of ptr = %u\n",ptr);
 	ptr--; // (ptr = ptr - 1)
 	printf("The value of ptr = %u\n",ptr);
 	return 0;
 	*/
 	
 }
